# Creates a transformation matrix for stretching

Creates a transformation matrix for stretching by a factor of r along
the x axis.

## Usage

``` r
rigid.stretch(r)
```

## Arguments

- r:

  stretching factor
