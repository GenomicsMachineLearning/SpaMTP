# Creates a transformation matrix for rotation

Creates a transformation matrix for clockwise rotation by 'alpha'
degrees

## Usage

``` r
rigid.rot(alpha = 0, forward = TRUE)
```

## Arguments

- alpha:

  rotation angle

- forward:

  should the rotation be done in forward direction?
