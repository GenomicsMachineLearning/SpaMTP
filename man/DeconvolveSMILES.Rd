% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SMILESStructure.R
\name{DeconvolveSMILES}
\alias{DeconvolveSMILES}
\title{Decompose explicit functional groups in SMILES strings}
\usage{
DeconvolveSMILES(
  smiles,
  backend = c("auto", "native"),
  strict = FALSE,
  workers = getOption("SpaMTP.smiles_workers", 1L)
)
}
\arguments{
\item{smiles}{Character vector of SMILES strings.}

\item{backend}{Currently \code{"auto"} and \code{"native"} both use SpaMTP's
native parser. The argument reserves a stable extension point for optional
chemistry toolkits.}

\item{strict}{Stop when any SMILES cannot be parsed. When \code{FALSE}, invalid
rows are returned with \code{structure_valid = FALSE} and missing features.}

\item{workers}{Number of forked workers used for unique SMILES on platforms
supporting \code{parallel::mclapply()}. The default is controlled by
\code{options(SpaMTP.smiles_workers = 1)}.}
}
\value{
A data frame with functional-group counts, ion-mode scores, proposed atom
sites, and a human-readable \code{structure_evidence} field.
}
\description{
A lightweight, dependency-free SMILES graph parser identifies common
metabolite functional groups and derives interpretable positive-ion,
negative-ion, alkali-binding, and neutral-mass priors. The parser is intended
for candidate prioritisation rather than pKa, proton-affinity, or quantum
chemistry calculations. Stereochemistry is accepted but does not change the
counts.
}
