% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BinSMData.R
\name{bin_cardinal}
\alias{bin_cardinal}
\title{This function bins spectral data generated by cardinal into a Matrix object (rather than a matter object)}
\usage{
bin_cardinal(
  x,
  ref,
  spectra = "intensity",
  index = "mz",
  method = c("sum", "mean", "max", "min", "linear", "cubic", "gaussian", "lanczos"),
  resolution = NA,
  tolerance = NA,
  units = c("ppm", "mz"),
  mass.range = NULL
)
}
\arguments{
\item{x}{The spectral data to be binned, typically a matrix or data frame.}

\item{ref}{A vector of reference mass-to-charge (m/z) values for binning. If left unspecified, mass range will be used to generate reference peaks.}

\item{spectra}{Character string specifying the column name for spectral intensity values (default = "intensity").}

\item{index}{Character string specifying the column name for the m/z values (default = "mz").}

\item{method}{A character vector specifying the binning method. Options include \code{"sum"}, \code{"mean"}, \code{"max"}, \code{"min"}, \code{"linear"}, \code{"cubic"}, \code{"gaussian"}, and \code{"lanczos"}. If not specified default method used is "sum".}

\item{resolution}{Numeric value specifying the resolution for binning. If assigned as \code{NA}, the resolution will be automatically caculated based on the data (default = NA).}

\item{tolerance}{Numeric value specifying the tolerance for m/z matching (default = \code{NA}).}

\item{units}{Character string specifying the units for resolution. Options are \code{"ppm"} or \code{"mz"} (default = "ppm").}

\item{mass.range}{Numeric vector of length two specifying the lower and upper bounds of the mass range for binning. If set to NULL will use the entire range to bin (default = NULL).}
}
\value{
A binned spectral data object with the same structure as the input, but with modified spectral data according to the specified binning parameters.
}
\description{
This function bins spectral data generated by cardinal into a Matrix object (rather than a matter object)
}
\examples{
# used as a helper function for LoadSM
}
