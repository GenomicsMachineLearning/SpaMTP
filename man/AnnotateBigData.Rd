% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MZAnnotation.R
\name{AnnotateBigData}
\alias{AnnotateBigData}
\title{Annotates vector of m/z values}
\usage{
AnnotateBigData(
  mzs,
  db = NULL,
  ppm_error = NULL,
  adducts = NULL,
  polarity = NULL,
  tof_resolution = 30000,
  verbose = TRUE,
  maldi_matrix = NULL,
  database_version = "latest",
  database_source = c("auto", "spamtpdb"),
  database_local_dir = NULL,
  ...
)
}
\arguments{
\item{mzs}{Vector containing m/z values for annotation.}

\item{db}{Reference metabolite dataset in the form of a data.frame. SpaMTP
provides four pre-cleaned databases (\code{HMDB_db}, \code{Lipidmaps_db},
\code{Chebi_db}, \code{GNPS_db}). May be \code{NULL} when a pre-built \code{index} is supplied
through \code{...}.}

\item{ppm_error}{Mass tolerance in ppm. If \code{NULL}, a strict 5 ppm maximum
is used (or a smaller value inferred from \code{tof_resolution}). Set to zero
for exact numerical matches.}

\item{adducts}{Optional adduct names/notations; see \code{AdductRules()}. If
\code{NULL}, use the complete rule space selected from \code{maldi_matrix}, or all
validated general rules for the selected polarity when no matrix is given.}

\item{polarity}{Character string defining the ion mode. When \code{NULL}, infer
it from a supplied index or MALDI matrix profile, otherwise use positive.}

\item{tof_resolution}{Instrument resolving power retained for compatibility;
it can only tighten, not widen, the default 5 ppm mass-accuracy threshold.}

\item{verbose}{Boolean indicating whether to show the message. If TRUE the message will be show, else the message will be suppressed (default = TRUE).}

\item{maldi_matrix}{Optional MALDI matrix/reagent profile used for automatic
rule selection. The \code{adducts} argument remains an optional restriction.}

\item{database_version}{SpaMTPdb/RaMP version used when \code{db = NULL}.}

\item{database_source}{Database source used when \code{db = NULL}; see
\code{\link[=LoadSpaMTPDatabase]{LoadSpaMTPDatabase()}}.}

\item{database_local_dir}{Optional staged SpaMTPdb resource directory.}

\item{...}{Additional indexed annotation/scoring arguments passed to
\code{annotateTable()}, such as \code{index}, \code{rules}, or \code{ms1_spectrum}.}
}
\value{
A data.frame containing all successfully annotated m/z values, with their corresponding annotation.
}
\description{
This function assigns each valid m/z peak with one/multiple metabolite names based on the mass difference between the observed value and the theoretical value documented in the reference database.
This function is to be used when dealing with large datasets as a preprocessing step. Users can annotate m/z values first and then subset their data accordinly before loading it into a SpaMTP Seurat Object.
}
\examples{
utils::str(formals(AnnotateBigData))
#cardinal <- readImzML("./Test_Data/Spotted/test_data1")
#mzs <- data.frame(Cardinal::featureData(cardinal))$mz
#results <- AnnotateBigData(mzs, db = HMDB_db, ppm_error = 3, adducts = c("M-H", "M+Cl"), polarity = "negative")
#cardinal_subset <- Cardinal::subset(cardinal, mz \%in\% results$observed_mz)
#SpaMTP_data <- CardinalToSeurat(cardinal_subset)
}
