% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PathwayAnalysis.R
\name{PathwaysPerRegion}
\alias{PathwaysPerRegion}
\title{Plotting of differentially expressed metabolic pathways per grouping identity}
\usage{
PathwaysPerRegion(
  SpaMTP,
  ident,
  polarity = NULL,
  assay = "Spatial",
  slot = "counts",
  fisherpathwaydf = NULL,
  tof_resolution = 30000,
  ppm_error = NULL,
  pval_cutoff = NULL,
  verbose = TRUE
)
}
\arguments{
\item{SpaMTP}{A seurat object contains spatial metabolomics/transcriptomics features or both.}

\item{ident}{Character string defining the metadata column used for grouping.}

\item{polarity}{The polarity of the MALDI experiment. Inputs must be either NULL, 'positive' or 'negative'. If NULL, pathway analysis will run in neutral mode (default = NULL).}

\item{assay}{Character string defining the SpaMTP assay to extract intensity values from (default = "SPM").}

\item{slot}{Character string specifying which slot to pull the m/z inensity values from (default = "counts").}

\item{tof_resolution}{is the tof resolution of the instrument used for MALDI run, calculated by ion \verb{[ion mass,m/z]}/\verb{[Full width at half height]} (default = 30000).}

\item{ppm_error}{is the parts-per-million error tolerance of matching m/z value with potential metabolites (default = NULL).}
}
\value{
A list consists of a ggplot2 object and a dataframe containing the set enrichment results
}
\description{
Plotting of differentially expressed metabolic pathways per grouping identity
}
\examples{
# PathwaysPerRegion(SpaMTP, ident = "clusters", polarity = "positive")
}
