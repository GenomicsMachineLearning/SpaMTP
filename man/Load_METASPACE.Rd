% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/METASPACEr.R
\name{Load_METASPACE}
\alias{Load_METASPACE}
\title{Load METASPACE Data and Create Seurat Object}
\usage{
Load_METASPACE(
  dataset_id,
  fdr = 0.1,
  database = c("HMDB", "v4"),
  api_key = NULL,
  relative = FALSE,
  transform = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dataset_id}{Character string of the METASPACE dataset ID.}

\item{fdr}{Numeric, the FDR level threshold (default = 0.1).}

\item{database}{Character vector, specifying the database name and version (default = c("HMDB", "v4")).}

\item{api_key}{Optional character string, your METASPACE API key (default = NULL).}

\item{relative}{Logical, if TRUE, normalizes pixel intensities (0-1 range) (default = FALSE).}

\item{transform}{Boolean specifiying whether to transform/flip the image (default = FALSE).}

\item{verbose}{Boolean indicating whether to show the message. If TRUE the message will be show, else the message will be suppressed (default = TRUE).}
}
\value{
A Seurat object initialized with the METASPACE data in the 'Spatial' assay.
}
\description{
Downloads METASPACE data, converts it to a feature matrix, and packages it into a Seurat object with spatial metadata.
}
\examples{
# Load_METASPACE(dataset_id = "2020-12-07_03h16m14s", fdr = 0.1, database= c("HMDB", "v4"), relative = TRUE)
}
