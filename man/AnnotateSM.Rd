% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MZAnnotation.R
\name{AnnotateSM}
\alias{AnnotateSM}
\title{Annotates m/z values stored in a SpaMTP Object}
\usage{
AnnotateSM(
  data,
  db = NULL,
  assay = "Spatial",
  raw.mz.column = "raw_mz",
  ppm_error = NULL,
  adducts = NULL,
  polarity = NULL,
  tof_resolution = 30000,
  filepath = NULL,
  return.only.annotated = TRUE,
  save.intermediate = TRUE,
  min_score = 0,
  verbose = TRUE,
  maldi_matrix = NULL,
  database_version = "latest",
  database_source = c("auto", "spamtpdb"),
  database_local_dir = NULL,
  ...
)
}
\arguments{
\item{data}{Seurat Spatial Metabolomic Object containing m/z values for annotation.}

\item{db}{Reference metabolite dataset in the form of a data.frame. When
\code{NULL}, the versioned SpaMTPdb \code{chem_props} table is used, unless a
pre-built \code{index} is supplied through \code{...}. Versioned resources are loaded
from SpaMTPdb and may be staged locally for offline use.}

\item{assay}{Character string defining the Seurat assay which contains the mz counts being annotated (default = "Spatial").}

\item{raw.mz.column}{Character string defining the Seurat assay slot which contains the raw mz values, this is without the 'mz-' and are a vector of integers. This is setup by default when running the cardinal_to_seurat() function (default = "raw_mz").}

\item{ppm_error}{Mass tolerance in ppm. If \code{NULL}, a strict 5 ppm maximum
is used (or a smaller value inferred from \code{tof_resolution}). Set to zero
for exact numerical matches.}

\item{adducts}{Optional adduct names/notations; see \code{AdductRules()}. If
\code{NULL}, use the complete rule space selected from \code{maldi_matrix}, or all
validated general rules for the selected polarity when no matrix is given.}

\item{polarity}{Character string defining the ion mode. When \code{NULL}, use the
MALDI matrix profile default, a supplied index's mode, or positive mode
when neither is available.}

\item{tof_resolution}{Instrument resolving power retained for compatibility;
it can only tighten, not widen, the default 5 ppm mass-accuracy threshold.}

\item{filepath}{Character string of the directory to store the _annotated_mz_peaks.csv. If set to NULL no dataframe will be saved (default = NULL).}

\item{return.only.annotated}{Boolean value indicating if the annotated Seurat Object should only include m/z values that were successfully annotated (default = TRUE).}

\item{save.intermediate}{Boolean indicating whether to store the scored
annotation result and its pipeline/RaMP provenance in
\verb{@tools$mz_annotation}. A compatibility copy is retained in
\verb{@tools$db_3} (default = TRUE).}

\item{min_score}{Minimum annotation score retained in the stored candidate
table. The default \code{0} keeps all ppm-valid candidates so downstream
pathway functions can apply a user-defined threshold without re-running
annotation.}

\item{verbose}{Boolean indicating whether to show the message. If TRUE the message will be show, else the message will be suppressed (default = TRUE).}

\item{maldi_matrix}{Optional MALDI matrix or derivatization reagent name.
When supplied, SpaMTP selects validated matrix-specific rules
automatically. \code{adducts} is optional and only restricts that automatic
search space when explicitly supplied.}

\item{database_version}{SpaMTPdb/RaMP version used when \code{db = NULL}.}

\item{database_source}{Database source used when \code{db = NULL}; see
\code{\link[=LoadSpaMTPDatabase]{LoadSpaMTPDatabase()}}.}

\item{database_local_dir}{Optional staged SpaMTPdb resource directory.}

\item{...}{Additional indexed annotation/scoring arguments passed to
\code{annotateTable()}, such as \code{index}, \code{rules}, \code{ms1_spectrum},
\code{infer_structure}, \code{structure_backend}, \code{structure_workers}, or
\code{min_structure_score}.}
}
\value{
A Seurat Object with m/z values annotated. These annotations are stored in the relative assay's meta.data (e.g. SeuratObj\verb{[["Spatial"]][[]]})
}
\description{
This function assigns each valid m/z peak with one/multiple metabolite names based on the mass difference between the observed value and the theoretical value documented in the reference database.
Versioned RaMP, HMDB, LIPID MAPS, ChEBI, and GNPS resources are provided by
SpaMTPdb and can be combined with user-supplied reference tables.
}
\examples{
utils::str(formals(AnnotateSM))
# HMDB_db <- load("data/HMDB_1_names.rds")
# Annotated_SeuratObj <- AnnotateSM(SeuratObj, HMDB_db)
}
