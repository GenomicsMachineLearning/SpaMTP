# Creates a transformation matrix that translates an object in 2D

Creates a transformation matrix that translates an object in 2D

## Usage

``` r
translate(translate.x, translate.y)
```

## Arguments

- translate.x, translate.y:

  translation of x, y coordinates
